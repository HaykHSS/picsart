function findlastindex(arr, callback){
    for(let i = arr.length - 1; i >= 0; i--){
       let result = callback(arr[i],i,arr);
        if(result){
            return i;
        }        
    }
    return -1;
}
