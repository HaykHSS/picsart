function sort(arr){
    for(let i = 0; i < arr.length;i++){
        for(let k = 0; k < arr.length -i -1;k++){
            if(String(arr[k]) > String(arr[k+1])){
                let temp = arr[k];
                arr[k] = arr[k+1];
                arr[k+1] = temp;
            }
        }
    }
    return arr;
}
