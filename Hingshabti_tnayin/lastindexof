function lastindexof(arr, item){
    for(let i = arr.length-1; i >=0; i--){
	if(arr.hasOwnProperty(i)){
       	     if(item === arr[i]){			
                 return i;
             }
	}
    }
    return -1;
}
